machine: W1 (stills-runner; note: this box is Linux, ran flow_driver.py Playwright path)
rows_worked: 63 (10/10 stills generated, QC pass, marker cleared, pushed)
minutes_per_video: 63=~14 min of Flow time for 11 gens
stills_generated / rerolled: 11 / 1
reroll_reasons: 1x s8 transient composer fail (retry succeeded); s5 tear-line streak
  found AFTER my set — L1 revised the s5 prompt mid-session and is rerolling s5/s6
  itself, so W1 stood down on s5 rather than fight for the profile
errors_hit_and_fix: stale SingletonLock after EVERY driver gen (Chrome leaves
  Singleton* symlinks; next launch aborts) -> added clear_stale_lock() to
  gen_stills_flow.py: rm Singleton* only when no /proc cmdline holds mbm-flow-profile;
  pkill -f mbm-flow-profile SELF-KILLS the Bash shell running it (pattern matches own
  cmdline) -> kill by PID from a /proc scan instead;
  L1 session shares this working tree AND the Flow profile -> lock contention + an
  uncommitted PROMPTS.md revision under my feet; used pull --rebase --autostash
biggest_token_waster: diagnosing the singleton-lock loop (4 failed batch starts)
what_should_change: (1) flow_driver.py launch() should clear stale Singleton* itself
  when no live process holds the profile — every caller re-solves this; (2) one Flow
  profile per machine is fine, but two sessions on ONE machine need a lock handshake
  or one session must own all Flow work; (3) STILLS-RUNNER relay assumes the prep
  machine moves on — L1 rerolling art after the marker was posted duplicates W1 work
tokens_estimate: ~35% of context for the one folder (half of it lock debugging)
