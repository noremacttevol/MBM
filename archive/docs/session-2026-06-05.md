# MBM — Session Log 2026-06-05

## What Was Built

### Flask Web App (SCRAPPED)
Built a Python Flask + SQLite web app running on localhost:5050. Scrapped per Cameron's direction — wrong platform for a mobile-first product.

### React Native / Expo Mobile App (CURRENT — at ~/Desktop/MBM-mobile/)
Rebuilt as a standalone mobile app using Expo. No backend server required. Runs on a real phone via Expo Go.

**File structure:**
```
~/Desktop/MBM-mobile/
├── App.js                        ← Navigation root
├── src/
│   ├── db/
│   │   ├── seed.js               ← 30 content items (MILK / BRIDGE / MAINTENANCE)
│   │   └── database.js           ← expo-sqlite, auto-seeds on first launch
│   ├── engine/
│   │   └── router.js             ← Hidden routing, escalation, safety valve
│   └── screens/
│       ├── HookScreen.js         ← "He Is Risen" animation
│       ├── OnboardScreen.js      ← 5-choice onboarding question
│       └── FeedScreen.js         ← Content cards + 5-item screen time cap
```

**To run on phone:**
1. Install Expo Go on your phone
2. `cd ~/Desktop/MBM-mobile && npx expo start`
3. Scan the QR code

## Routing Logic (baked in, hidden from user)
| Answer | Profile | Feed |
|--------|---------|------|
| A — "parents told me" | MEMBER | MAINTENANCE |
| B — "questioned it" | SKEPTIC | BRIDGE |
| C — "felt peace" | SEEKER | MILK |
| D — "never thought about it" | SECULAR | MILK |
| E — free text | NLP keyword match | MILK / BRIDGE / MAINTENANCE |

**Escalation:** "Take me deeper" button moves MILK → BRIDGE → MAINTENANCE (pull-based only)
**Safety valve:** "Keep it simple" resets anyone back to MILK + comfort resonance

## Session Notes
- Claude built Flask first — wrong platform, wasted time
- Cameron ended session to work with Hermes
- All code is saved and functional at ~/Desktop/MBM-mobile
